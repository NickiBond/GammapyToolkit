This Toolkit is used to run gammapy using VERITAS data (which has been processed with VEGAS).

- It can be run by using either
    - DL3toDL5.py on the command line,
    -  the GUI: "RunGammaPyGUI.py". 

- It also has a couple of other useful scripts e.g. for Comparing folders of fits files.
